; Dev override: re-check file mtimes on every request so PHP edits are picked up immediately.
; Default is 60s, which forces a `docker compose restart` after each backend change.
opcache.revalidate_freq=0
opcache.validate_timestamps=1
